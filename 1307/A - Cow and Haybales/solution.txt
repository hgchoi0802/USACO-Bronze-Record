T = int(input())
 
for i in range(T):
    n, d = map(int, input().split())
    a = list(map(int, input().split()))
 
    for j in range(1, n):
        can = min(a[j], d // j)
        a[0] += can
        d -= can * j
 
    print(a[0])